# Chain-of-Retrieval Augmented Generation

CoRAG的方法为：

1. 使用链式推理的方法,生成多条推理路径。每条推理路径其实是逐步生成子查询(依据该路径的历史)、子回答的过程,并在最后生成最终的问题答案。对于一个问题生成的所有路径,计算最终答案和正确答案的对数似然,选择其中对数似然分数最高的结果,其他的直接“拒绝”。这就是拒绝采样。
2. 使用上一步的结果来增强初始的QA数据集。在统一的多任务学习框架下，使用标准的下一个token预测目标在增强数据集上对基线模型进行微调。
该模型同时训练三个任务：下一个子查询预测、子答案预测和最终答案预测。作者采用与检索链生成过程相同的提示模板，唯一区别在于我们还将原始查询Q的top检索文档D1:k作为最终答案预测任务的输入。
$$
\begin{aligned}
L_{\text{sub\_query}} &= -\log \mathrm{P}\left(Q_i \mid Q, Q_{<i}, A_{<i}\right), \quad i \in [1, L] \\
L_{\text{sub\_answer}} &= -\log \mathrm{P}\left(A_i \mid Q_i, D_{1:k}^{(i)}\right), \quad i \in [1, L] \\
L_{\text{final\_answer}} &= -\log \mathrm{P}\left(A \mid Q, Q_{1:L}, A_{1:L}, D_{1:k}\right)
\end{aligned}
$$
3. 在输出阶段使用3个不同的采样方式来平衡结果准确度和性能。

- 贪婪搜索： 每次只选概率最高的一步，速度快。
- Best-of-N： 生成 N 条路径选最好的。
- 树搜索 ： 进行更复杂的搜索和回溯，通过 rollout 模拟未来结果来决定当前步骤，这能带来综合最好的结果但计算量最大。

# 3个问题

1. CoRAG在检索侧的代码被REAP完全拿去用了,这个没有问题吗？
2. 申请Llama3.1的使用权限被拒绝了,那边似乎对中俄地区的用户有歧视。也可以通过换账号填假信息申请的办法把它搞来用,但是论文会不会有被追责的可能？(虽然REAP和CoRAG没有管这么多)
3. REAP用的是CoRAG的语料库。语料库大小本身有10个g,但是向量数据库他给出了70个g的文件。一方面把这么大的东西放进服务器里有点心理压力,另一方面理论上用这个向量数据库应该要把它全部存进内存/显存,设备会不会也有点压力？在他的代码里面他默认处理是用了4张卡。
```python
def __init__(
            self, index_dir: str,
            model_name_or_path: str = '/path/to/e5-large-v2',
            verbose: bool = False,
    ):
        self.model_name_or_path = model_name_or_path
        self.index_dir = index_dir
        self.verbose = verbose 

        # 这一段逻辑写得很死，显然是为了特定的服务器环境定制的。
        physical_gpu_count: int = torch.cuda.device_count()
        
        # 强制指定使用 ID 为 4, 5, 6, 7 的显卡。
        _explicitly_desired_gpu_ids: List[int] = [4, 5, 6, 7] 

        self.gpu_ids_to_use: List[int] = []
        if physical_gpu_count == 0:
            logger.warning("No physical GPUs available...")
        else:
            for gid in _explicitly_desired_gpu_ids:
                if 0 <= gid < physical_gpu_count:
                    self.gpu_ids_to_use.append(gid)
                else:
                    logger.warning(f"Desired GPU ID {gid} is not available (total physical GPUs: {physical_gpu_count}). It will be skipped.")
            
            if not self.gpu_ids_to_use:
                logger.warning(f"None of the specifically desired GPUs ({_explicitly_desired_gpu_ids}) are available or valid. "
                               "Faiss will attempt to run on CPU if faiss-cpu is installed (and no specific GPUs were usable).")
            else:
                logger.info(f"Script configured to use the following GPUs for Faiss: {self.gpu_ids_to_use}.")
        
        num_selected_gpus: int = len(self.gpu_ids_to_use)
        # -----------------------------------
```